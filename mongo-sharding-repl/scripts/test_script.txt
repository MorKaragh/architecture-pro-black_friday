# Подключитесь к контейнеру mongodb-config1
docker exec -it mongodb-config1 mongosh --port 27019

# В mongosh выполните команду инициализации replica set:
rs.initiate({
  _id: "configReplSet",
  configsvr: true,
  members: [
    { _id: 0, host: "mongodb-config1:27019" },
    { _id: 1, host: "mongodb-config2:27019" },
    { _id: 2, host: "mongodb-config3:27019" }
  ]
})